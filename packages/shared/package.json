{
  "name": "@stack/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bunx tsc -p tsconfig.build.json",
    "clean": "rimraf dist || rd /s /q dist",
    "prepare": "bunx tsc -p tsconfig.build.json",
    "watch": "bunx tsc -p tsconfig.build.json --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
